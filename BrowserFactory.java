package package1;

import org.openqa.selenium.WebDriver;

public class BrowserFactory {
   public static void  launchBrowse(String browserName) {
	   
	   WebDriver driver=null;
	   if(browserName.equalsIgnoreCase("chrome")) {
		   
		   
	   }
	   
	   
	   
	   
   }
	
	
}
